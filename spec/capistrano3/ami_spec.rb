require 'spec_helper'

describe Capistrano3::Ami do
  it 'has a version number' do
    expect(Capistrano3::Ami::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
